@model IEnumerable<SafeTravel.Models.Region>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.r_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.countries)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.precautions)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.info)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.r_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.countries)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.precautions)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.info)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.region_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.region_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.region_id })
        </td>
    </tr>
}

</table>
